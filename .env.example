# Local MSYS2 connection settings for tools/install_pi_initial_deployment.sh.
# Copy to .env or let the installer create .env. Never commit real credentials.
PI_HOST=192.168.68.110
PI_USER=pi
PI_PASS=
